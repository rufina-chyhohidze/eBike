stages:
  - login
#  - rg-and-vm-setup

azure-login:
  stage: login
  image: ubuntu:22.04
  before_script:
    - apt-get update
    - apt-get install -y curl openssh-client
    - curl -sL https://aka.ms/InstallAzureCLIDeb | bash
  script:
    - echo "🔐 Logging into Azure..."
    - az login --service-principal -u "$CLIENT_ID" -p "$CLIENT_SECRET" --tenant "$TENANT_ID" &>/dev/null
    - az account show && echo "✅ Azure login successful"
    - chmod +x ./azure-config/create-virtual-machine.sh
    - ./azure-config/create-virtual-machine.sh

#azure-rg-and-vm-setup:
#  stage: rg-and-vm-setup
#



#stages:
#  - login
#  - deploy
#
#image: mcr.microsoft.com/azure-cli:latest
#
#before_script:
#  - apt-get update
#  - apt-get install -y openssh-client  # for ssh-keygen
#  - az --version
#  - ssh-keygen -V  # confirm ssh is installed
#
#azure-login:
#  stage: login
#  script:
#    - echo "🔐 Logging into Azure..."
#    - az login --service-principal -u "$CLIENT_ID" -p "$CLIENT_SECRET" --tenant "$TENANT_ID" &>/dev/null
#    - az account show && echo "✅ Azure login successful"
#
#deploy-vm:
#  stage: deploy
#  script:
#    -
#    - chmod +x ./azure-config/create-virtual-machine.sh  # Make sure the script is executable
#    - ./azure-config/create-virtual-machine.sh  # Execute the script