stages:
#  - build
#  - test
  - pre-deploy # Creates resource group, virtual machine and SQL DataBase for deployment using terraform (OpenTofu) if they don't exist
#  - deploy
# - destroy

# for destory stage: tofu apply -destroy --auto-approve

variables:
  COMPOSE_BAKE: true # Better build performance

#build_application:
#  stage: build
#  tags:
#    - azure
#    - shell
#  script:
#    - echo "Running build stage in pipeline $(date)" >> /var/log/cd-gitlab-pipeline.log
#    - echo "========== BUILD STAGE =========="
#    - docker compose build --no-cache
#    - echo "========== BUILD STAGE COMPLETED SUCCESSFULLY =========="



#deploy_application:
#  stage: deploy
#  tags:
#    - azure
#    - shell
#  script:
#    - echo "Running deploy stage in pipeline $(date)" >> /var/log/cd-gitlab-pipeline.log
#    - echo "========== DEPLOY STAGE =========="
#    - echo "========== DEPLOY STAGE COMPLETED SUCCESSFULLY =========="


#test_application:
#  stage: test
#  tags:
#    - azure
#    - setup
#  script:
#    - echo "========== TEST STAGE =========="
#    - docker compose up team18-integration-test
#    - echo "========== TEST STAGE COMPLETED SUCCESSFULLY =========="

azure-deployment-setup:
  stage: pre-deploy
  image: anir333/team18-int4:latest
  tags:
    - azure
    - docker
  script:
    - echo "========== PRE-DEPLOY STAGE =========="
    - cd ./azure-config/deployment/ || exit 1
    - chmod +x ./pre-deploy-check.sh
    - chmod +x ./azure-login.sh
    - ./azure-login.sh
    - ./pre-deploy-check.sh
    - echo "========== PRE-DEPLOY STAGE COMPLETED SUCCESSFULLY =========="


#deploy_application:
#  stage: deploy
#  image: anir333/integration4:cicd1
#  script:
#    - echo "🔐 Logging into Azure..."
#    - az login --service-principal -u "$CLIENT_ID" -p "$CLIENT_SECRET" --tenant "$TENANT_ID" &>/dev/null
#    - az account show &>/dev/null && echo "✅ Azure login successful"
#    - chmod +x ./azure-config/create-virtual-machine.sh
#    - ./azure-config/create-virtual-machine.sh
#




# i need:


#stages:
#  - login
#
#azure-login:
#  stage: login
#  image: mcr.microsoft.com/azure-cli
#  script:
#    - echo "🔐 Logging into Azure..."
#    - az login --service-principal -u "$CLIENT_ID" -p "$CLIENT_SECRET" --tenant "$TENANT_ID" &>/dev/null
#    - az account show && echo "✅ Azure login successful"
#    - chmod +x ./azure-config/create-virtual-machine.sh
#    - ./azure-config/create-virtual-machine.sh