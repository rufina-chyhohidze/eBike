- name: Provision & Deploy Java App
  hosts: web
  become: yes

  vars:
    # 1) Where on the VM to unpack your application
    app_dir: /home/team18-deploy/project

    # 2) Temp tarball path (on both control machine and VM)
    tarball: /tmp/myapp.tar.gz

    # 3) Path from ansible/ back to your project root:
    #    ansible/ → gitlab-runner-setup/ → azure-config/ → ebiketeam18/
    project_root: "{{ playbook_dir }}/../../../"

  tasks:
    ####################################################################
    # 1) INSTALL DOCKER CE
    ####################################################################
    - name: Install yum-utils
      dnf:
        name: yum-utils
        state: present

    - name: Add Docker CE repo
      command: >
        yum-config-manager
        --add-repo
        https://download.docker.com/linux/centos/docker-ce.repo
      args:
        creates: /etc/yum.repos.d/docker-ce.repo

    - name: Install Docker CE packages
      dnf:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
        state: latest
        update_cache: yes

    - name: Ensure Docker service is running & enabled
      service:
        name: docker
        state: started
        enabled: yes


    ####################################################################
    # 2) REMOVE ANY OLD APP
    ####################################################################
    - name: Remove old application directory if it exists
      file:
        path: "{{ app_dir }}"
        state: absent

    ####################################################################
    # 3) PACKAGE PROJECT INTO A TARBALL
    ####################################################################
    - name: Create project tarball on control machine
      local_action:
        module: command
        argv:
          - tar
          - czf
          - "{{ tarball }}"
          - -C
          - "{{ project_root }}"
          - .
      run_once: true
      delegate_to: localhost

    ####################################################################
    # 4) COPY & UNPACK ON VM
    ####################################################################
    - name: Copy tarball to VM
      copy:
        src: "{{ tarball }}"
        dest: "{{ tarball }}"
        mode: "0644"

    - name: Ensure application directory exists
      file:
        path: "{{ app_dir }}"
        state: directory
        mode: "0755"

    - name: Unpack tarball into {{ app_dir }}
      unarchive:
        src: "{{ tarball }}"
        dest: "{{ app_dir }}"
        remote_src: yes

    - name: Remove tarball from VM
      file:
        path: "{{ tarball }}"
        state: absent

    ####################################################################
    # 5) TEAR DOWN EXISTING CONTAINERS (via CLI)
    ####################################################################
    - name: Bring down any running Docker containers and remove images
      shell: |
        CONTAINERS=$(docker ps -aq)
        IMAGES=$(docker images -aq)
        if [ -n "$CONTAINERS" ]; then
          docker rm -f $CONTAINERS
        fi
        if [ -n "$IMAGES" ]; then
          docker rmi -f $IMAGES
        fi
      args:
        chdir: "{{ app_dir }}"


    ####################################################################
    # 6) START NEW DOCKER-COMPOSE STACK (via CLI)
    ####################################################################
    - name: Launch application via docker-compose
      shell: docker compose up -d --build
      args:
        chdir: "{{ app_dir }}"
